$(document).on('turbolinks:load', function() {
  let image_field = $("#image_field");
  
  $("#upload_field").on('change', 'input[type="file"]', function(e) {
    let file = e.target.files[0],
  })
});

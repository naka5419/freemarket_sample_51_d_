%body
  .user-signup

    .signup-wrapper

      %header.signup-header

        %h1

          = link_to root_path do
            = image_tag '//www-mercari-jp.akamaized.net/assets/img/common/common/logo.svg?3160895340', alt: 'mercari', height: '', width: '', class: 'signup-header-img'

        %nav.progress-ber

          %ol.clearfix
            %li.active
              会員情報
              .progress-status.back-active
                .after
            %li
              電話番号認証
              .progress-status
                .after
                .before
            %li
              お届け先住所入力
              .progress-status
                .after
                .before
            %li
              支払い方法
              .progress-status
                .after
                .before
            %li
              完了
              .progress-status
                .before

      %main.signup-main

        %section.signup-main__container

          %h2.signup-head 会員情報入力

          .signup-inner

            = form_with(model: resource, url: registration_path(resource_name), local: true) do |f|

              .form
                = devise_error_messages!

                .form__group#first
                  = f.label :ニックネーム
                  %span.form__group--require 必須
                  = f.text_field :nickname, autofocus: true,autocomplete: "nickname", class: 'field-input', placeholder: "例）メルカリ太郎"

                .form__group
                  = f.label :メールアドレス
                  %span.form__group--require 必須
                  = f.email_field :email, autofocus: true,autocomplete: "email", class: 'field-input', placeholder: "PC・携帯どちらでも可"

                .form__group
                  = f.label :パスワード
                  %span.form__group--require 必須
                  = f.password_field :password, autocomplete: "off", class: 'field-input', placeholder: "6文字以上"

                .form__group
                  = f.label :パスワード確認
                  %span.form__group--require 必須
                  = f.password_field :password, autocomplete: "off", class: 'field-input', placeholder: "6文字以上"

                .form__group
                  %h3.text-left 本人確認
                  %p.text#identification
                    安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。

                .form__group
                  = f.label :"姓(全角)"
                  %span.form__group--require 必須
                  = f.text_field :firstname, autocomplete: "off", class: 'field-input', placeholder: "例）山田"

                .form__group
                  = f.label :"名(全角)"
                  %span.form__group--require 必須
                  = f.text_field :lastname, autocomplete: "off", class: 'field-input', placeholder: "例）彩"

                .form__group
                  = f.label :"姓カナ(全角)"
                  %span.form__group--require 必須
                  = f.text_field :firstname_kana, autocomplete: "off", class: 'field-input', placeholder: "例）ヤマダ"

                .form__group
                  = f.label :"名カナ(全角)"
                  %span.form__group--require 必須
                  = f.text_field :lastname_kana, autocomplete: "off", class: 'field-input', placeholder: "例）アヤ"

                -# 必須ではないため時間があれば実装するできなければ消去する
                -# .form__group
                -#   = f.label :"生年月日"
                -#   %span.form__group--require 必須
                -#   %br/

                -#   .birthday-select-wrap

                -#     .select-wrap
                -#       %select.select-default
                -#         %option{name: ""} --
                -#       %i.fas.fa-angle-down
                -#     %span 年

                -#     .select-wrap.month
                -#       %select.select-default
                -#         %option{value: ""} --
                -#       %i.fas.fa-angle-down
                -#     %span 月

                -#     .select-wrap.day
                -#       %select.select-default
                -#         %option{value: ""} --
                -#       %i.fas.fa-angle-down
                -#     %span 日

                %p.info-text#ash
                  ※本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。

                .form__group
                  .g-recaptcha.signup-recaptcha{ data: { sitekey: "6LfZGCYTAAAAAHoK-s7Lg5OdYg5Fq4susvALmZoc"}}
                    %div
                      %iframe{frameborder: "0", height: "78", name: "a-38r8qy4bksn8", role: "presentation", sandbox: "allow-forms allow-popups allow-same-origin allow-scripts allow-top-navigation allow-modals allow-popups-to-escape-sandbox", scrolling: "no", src: "https://www.google.com/recaptcha/api2/anchor?ar=2&k=6LfZGCYTAAAAAHoK-s7Lg5OdYg5Fq4susvALmZoc&co=aHR0cHM6Ly93d3cubWVyY2FyaS5jb206NDQz&hl=ja&v=v1558333958099&size=normal&cb=do3zentao7q1", width: "304"}
                    %textarea#g-recaptcha-response.g-recaptcha-response{name: "g-recaptcha-response", style: "width: 250px; height: 40px; border: 1px solid rgb(193, 193, 193); margin: 10px 25px; padding: 0px; resize: none; display: none;"}

                .form__group
                  %p.text-senter
                    「次へ進む」のボタンを押すことにより、
                    %a.link-blue(href="#")<> 利用規約
                    に同意したものとみなします

                = f.submit "次へ進む", class: 'btn-red btn-befault'

                = link_to "次へ進む(仮)" ,new_smse_path, class: "btn-red btn-befault"

                .form__group
                  %p.text-right.link-blue
                    = link_to user_session_path, class: "link-blue" do
                      本人情報の登録について
                    %i.fas.fa-angle-right

      = render 'layouts/signup-footer'


%body
  .user-signup4

    .signup-container

      %header.signup-header

        %h1

          %a(href="#")
            = image_tag '//www-mercari-jp.akamaized.net/assets/img/common/common/logo.svg?3160895340', alt: 'mercari', height: '', width: '', class: 'signup-header-img'

        %nav.progress-ber

          %ol.clearfix
            %li
              会員情報
              .progress-status.back-active
                .after.back-active
            %li
              電話番号認証
              .progress-status.back-active
                .before.back-active
                .after.back-active
            %li
              お届け先住所入力
              .progress-status.back-active
                .before.back-active
                .after.back-active
            %li
              支払い方法
              .progress-status.back-active
                .before.back-active
                .after.back-active
            %li.active
              完了
              .progress-status.back-active
                .before.back-active


      %main.signup-main

        %section.signup__container

          %h2.signup-head 会員登録完了

          .signup-inner

            -# = form_for [@user] do |f|
            -#   .signup-content

            -#     .form-group#first
            -#       = f.label :携帯電話の番号
            -#       -# %span.form-require 必須
            -#       = f.text_field :nickname, autofocus: true,autocomplete: "email", class: 'field-input', placeholder: "携帯電話の番号を入力"

            -#     %p.info-text
            -#       ※本人確認のため、携帯電話のSNS(ショートメッセージサービス)を利用して認証を行います。

            -#     = f.submit "次へ進む", class: 'btn-red btn-befault'

            -#     %p.info-text
            -#       ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。

            -#     .form-group
            -#       %p.text-right.rink-bule
            -#         %a.link-blue(href="#" terget="_blank")
            -#           電話番号の確認が必要な理由
            -#           %i.fas.fa-angle-right

      = render partial: 'footer'

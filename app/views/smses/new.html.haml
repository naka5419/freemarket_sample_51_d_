%body
  .user-signup

    .signup-wrapper

      %header.signup-header

        %h1

          = link_to new_user_registration_path do
            = image_tag '//www-mercari-jp.akamaized.net/assets/img/common/common/logo.svg?3160895340', alt: 'mercari', height: '', width: '', class: 'signup-header-img'

        %nav.progress-ber

          %ol.clearfix
            %li
              会員情報
              .progress-status.back-active
                .after.back-active
            %li.active
              電話番号認証
              .progress-status.back-active
                .before.back-active
                .after
            %li
              お届け先住所入力
              .progress-status
                .before
                .after
            %li
              支払い方法
              .progress-status
                .before
                .after
            %li
              完了
              .progress-status
                .before


      %main.signup-main

        %section.signup-main__container

          %h2.signup-head 電話番号の確認

          .signup-inner

            = form_with(local: true) do |f|
              .form

                .form__group#first
                  = f.label :携帯電話の番号
                  = f.number_field :phone_number, autofocus: true, autocomplete: "off", class: 'field-input', placeholder: "携帯電話の番号を入力"

                %p.info-text#black
                  ※本人確認のため、携帯電話のSNS(ショートメッセージサービス)を利用して認証を行います。

                = f.submit "次へ進む", class: 'btn-red btn-befault'

                = link_to "次へ進む(仮)" ,new_address_path, class: "btn-red btn-befault"

                %p.info-text
                  ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。

                .form__group
                  %p.text-right.rink-bule
                    = link_to new_user_registration_path, class: "link-blue" do
                      電話番号の確認が必要な理由
                      %i.fas.fa-angle-right

      = render partial: 'footer'
